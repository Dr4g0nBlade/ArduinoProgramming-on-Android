<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ImageView
        android:id="@+id/iv_avatar"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_margin="12dp"
        android:src="@mipmap/ic_no_avatar"/>


    <TextView
        android:id="@+id/tv_disp_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_avatar"
        android:layout_margin="6dp"
        android:layout_centerHorizontal="true"
        android:text="DISPLAY NAME"
        android:textSize="26sp" />

    <Button
        android:id="@+id/bt_setup_arduino"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_disp_name"
        android:text="setup arduino"/>
    <Button
        android:id="@+id/bt_pair_bt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/bt_setup_arduino"
        android:text="Pair android with arduino"/>

</RelativeLayout>